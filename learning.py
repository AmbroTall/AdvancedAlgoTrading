import numpy as np


a = np.array([0, 0.5, 1.0, 1.5, 2.0])

# Normal array
a = np.array(['a', 'b', 'c'])

# Generate range from 0 - 20 but skipping 2 digits while generating
a = np.arange(2, 20, 2)

# Convert the range into floating points
a = np.arange(8, dtype=float)
print(a)

# print(type(a))
